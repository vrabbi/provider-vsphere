---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: vspheresupervisors.supervisor.terasky.labs
spec:
  group: supervisor.terasky.labs
  names:
    categories:
    - crossplane
    - managed
    - vsphere
    kind: VSphereSupervisor
    listKind: VSphereSupervisorList
    plural: vspheresupervisors
    singular: vspheresupervisor
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VSphereSupervisor is the Schema for the VSphereSupervisors API.
          Provides a VMware vSphere Supervisor resource..
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VSphereSupervisorSpec defines the desired state of VSphereSupervisor
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cluster:
                    description: |-
                      The identifier of the compute cluster.
                      ID of the vSphere cluster on which workload management will be enabled.
                    type: string
                  contentLibrary:
                    description: |-
                      The identifier of the subscribed content library.
                      ID of the subscribed content library.
                    type: string
                  dvsUuid:
                    description: |-
                      The UUID of the distributed switch.
                      The UUID (not ID) of the distributed switch.
                    type: string
                  edgeCluster:
                    description: |-
                      The identifier of the NSX Edge Cluster.
                      ID of the NSX Edge Cluster.
                    type: string
                  egressCidr:
                    description: |-
                      CIDR blocks from which NSX assigns IP addresses used for performing SNAT from container IPs to external IPs.
                      CIDR blocks from which NSX assigns IP addresses used for performing SNAT from container IPs to external IPs.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  ingressCidr:
                    description: |-
                      CIDR blocks from which NSX assigns IP addresses for Kubernetes Ingresses and Kubernetes Services of type LoadBalancer.
                      CIDR blocks from which NSX assigns IP addresses for Kubernetes Ingresses and Kubernetes Services of type LoadBalancer.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  mainDns:
                    description: |-
                      The list of addresses of the primary DNS servers.
                      List of DNS servers to use on the Kubernetes API server.
                    items:
                      type: string
                    type: array
                  managementNetwork:
                    description: |-
                      The configuration for the management network which the control plane VMs will be connected to.
                      The configuration for the management network which the control plane VMs will be connected to.
                    items:
                      properties:
                        addressCount:
                          description: |-
                            Number of addresses to allocate. Starts from starting_address
                            Number of addresses to allocate. Starts from 'starting_address'
                          type: number
                        gateway:
                          description: |-
                            Gateway IP address.
                            Gateway IP address.
                          type: string
                        network:
                          description: |-
                            ID of the network. (e.g. a distributed port group).
                            ID of the network. (e.g. a distributed port group).
                          type: string
                        startingAddress:
                          description: |-
                            Starting address of the management network range.
                            Starting address of the management network range.
                          type: string
                        subnetMask:
                          description: |-
                            Subnet mask.
                            Subnet mask.
                          type: string
                      type: object
                    type: array
                  namespace:
                    description: |-
                      The list of namespaces to create in the Supervisor cluster
                      List of namespaces associated with the cluster.
                    items:
                      properties:
                        contentLibraries:
                          description: |-
                            The list of content libraries to associate with the namespace
                            A list of content libraries.
                          items:
                            type: string
                          type: array
                        name:
                          description: |-
                            The name of the namespace
                            The name of the namespace.
                          type: string
                        vmClasses:
                          description: |-
                            The list of virtual machine classes to add to the namespace
                            A list of virtual machine classes.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  podCidr:
                    description: |-
                      CIDR blocks from which Kubernetes allocates pod IP addresses. Minimum subnet size is 23.
                      CIDR blocks from which Kubernetes allocates pod IP addresses. Minimum subnet size is 23.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  searchDomains:
                    description: |-
                      List of DNS search domains.
                      List of DNS search domains.
                    items:
                      type: string
                    type: array
                  serviceCidr:
                    description: |-
                      CIDR block from which Kubernetes allocates service cluster IP addresses.
                      CIDR block from which Kubernetes allocates service cluster IP addresses.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  sizingHint:
                    description: |-
                      The size of the Kubernetes API server.
                      Size of the Kubernetes API server.
                    type: string
                  storagePolicy:
                    description: |-
                      The name of the storage policy.
                      The name of a storage policy associated with the datastore where the container images will be stored.
                    type: string
                  workerDns:
                    description: |-
                      The list of addresses of the DNS servers to use for the worker nodes.
                      List of DNS servers to use on the worker nodes.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cluster:
                    description: |-
                      The identifier of the compute cluster.
                      ID of the vSphere cluster on which workload management will be enabled.
                    type: string
                  contentLibrary:
                    description: |-
                      The identifier of the subscribed content library.
                      ID of the subscribed content library.
                    type: string
                  dvsUuid:
                    description: |-
                      The UUID of the distributed switch.
                      The UUID (not ID) of the distributed switch.
                    type: string
                  edgeCluster:
                    description: |-
                      The identifier of the NSX Edge Cluster.
                      ID of the NSX Edge Cluster.
                    type: string
                  egressCidr:
                    description: |-
                      CIDR blocks from which NSX assigns IP addresses used for performing SNAT from container IPs to external IPs.
                      CIDR blocks from which NSX assigns IP addresses used for performing SNAT from container IPs to external IPs.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  ingressCidr:
                    description: |-
                      CIDR blocks from which NSX assigns IP addresses for Kubernetes Ingresses and Kubernetes Services of type LoadBalancer.
                      CIDR blocks from which NSX assigns IP addresses for Kubernetes Ingresses and Kubernetes Services of type LoadBalancer.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  mainDns:
                    description: |-
                      The list of addresses of the primary DNS servers.
                      List of DNS servers to use on the Kubernetes API server.
                    items:
                      type: string
                    type: array
                  managementNetwork:
                    description: |-
                      The configuration for the management network which the control plane VMs will be connected to.
                      The configuration for the management network which the control plane VMs will be connected to.
                    items:
                      properties:
                        addressCount:
                          description: |-
                            Number of addresses to allocate. Starts from starting_address
                            Number of addresses to allocate. Starts from 'starting_address'
                          type: number
                        gateway:
                          description: |-
                            Gateway IP address.
                            Gateway IP address.
                          type: string
                        network:
                          description: |-
                            ID of the network. (e.g. a distributed port group).
                            ID of the network. (e.g. a distributed port group).
                          type: string
                        startingAddress:
                          description: |-
                            Starting address of the management network range.
                            Starting address of the management network range.
                          type: string
                        subnetMask:
                          description: |-
                            Subnet mask.
                            Subnet mask.
                          type: string
                      type: object
                    type: array
                  namespace:
                    description: |-
                      The list of namespaces to create in the Supervisor cluster
                      List of namespaces associated with the cluster.
                    items:
                      properties:
                        contentLibraries:
                          description: |-
                            The list of content libraries to associate with the namespace
                            A list of content libraries.
                          items:
                            type: string
                          type: array
                        name:
                          description: |-
                            The name of the namespace
                            The name of the namespace.
                          type: string
                        vmClasses:
                          description: |-
                            The list of virtual machine classes to add to the namespace
                            A list of virtual machine classes.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  podCidr:
                    description: |-
                      CIDR blocks from which Kubernetes allocates pod IP addresses. Minimum subnet size is 23.
                      CIDR blocks from which Kubernetes allocates pod IP addresses. Minimum subnet size is 23.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  searchDomains:
                    description: |-
                      List of DNS search domains.
                      List of DNS search domains.
                    items:
                      type: string
                    type: array
                  serviceCidr:
                    description: |-
                      CIDR block from which Kubernetes allocates service cluster IP addresses.
                      CIDR block from which Kubernetes allocates service cluster IP addresses.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  sizingHint:
                    description: |-
                      The size of the Kubernetes API server.
                      Size of the Kubernetes API server.
                    type: string
                  storagePolicy:
                    description: |-
                      The name of the storage policy.
                      The name of a storage policy associated with the datastore where the container images will be stored.
                    type: string
                  workerDns:
                    description: |-
                      The list of addresses of the DNS servers to use for the worker nodes.
                      List of DNS servers to use on the worker nodes.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cluster is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cluster)
                || (has(self.initProvider) && has(self.initProvider.cluster))'
            - message: spec.forProvider.contentLibrary is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.contentLibrary)
                || (has(self.initProvider) && has(self.initProvider.contentLibrary))'
            - message: spec.forProvider.dvsUuid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dvsUuid)
                || (has(self.initProvider) && has(self.initProvider.dvsUuid))'
            - message: spec.forProvider.edgeCluster is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.edgeCluster)
                || (has(self.initProvider) && has(self.initProvider.edgeCluster))'
            - message: spec.forProvider.egressCidr is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.egressCidr)
                || (has(self.initProvider) && has(self.initProvider.egressCidr))'
            - message: spec.forProvider.ingressCidr is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ingressCidr)
                || (has(self.initProvider) && has(self.initProvider.ingressCidr))'
            - message: spec.forProvider.mainDns is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.mainDns)
                || (has(self.initProvider) && has(self.initProvider.mainDns))'
            - message: spec.forProvider.managementNetwork is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.managementNetwork)
                || (has(self.initProvider) && has(self.initProvider.managementNetwork))'
            - message: spec.forProvider.podCidr is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.podCidr)
                || (has(self.initProvider) && has(self.initProvider.podCidr))'
            - message: spec.forProvider.searchDomains is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.searchDomains)
                || (has(self.initProvider) && has(self.initProvider.searchDomains))'
            - message: spec.forProvider.serviceCidr is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceCidr)
                || (has(self.initProvider) && has(self.initProvider.serviceCidr))'
            - message: spec.forProvider.sizingHint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.sizingHint)
                || (has(self.initProvider) && has(self.initProvider.sizingHint))'
            - message: spec.forProvider.storagePolicy is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.storagePolicy)
                || (has(self.initProvider) && has(self.initProvider.storagePolicy))'
            - message: spec.forProvider.workerDns is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.workerDns)
                || (has(self.initProvider) && has(self.initProvider.workerDns))'
          status:
            description: VSphereSupervisorStatus defines the observed state of VSphereSupervisor.
            properties:
              atProvider:
                properties:
                  cluster:
                    description: |-
                      The identifier of the compute cluster.
                      ID of the vSphere cluster on which workload management will be enabled.
                    type: string
                  contentLibrary:
                    description: |-
                      The identifier of the subscribed content library.
                      ID of the subscribed content library.
                    type: string
                  dvsUuid:
                    description: |-
                      The UUID of the distributed switch.
                      The UUID (not ID) of the distributed switch.
                    type: string
                  edgeCluster:
                    description: |-
                      The identifier of the NSX Edge Cluster.
                      ID of the NSX Edge Cluster.
                    type: string
                  egressCidr:
                    description: |-
                      CIDR blocks from which NSX assigns IP addresses used for performing SNAT from container IPs to external IPs.
                      CIDR blocks from which NSX assigns IP addresses used for performing SNAT from container IPs to external IPs.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  id:
                    type: string
                  ingressCidr:
                    description: |-
                      CIDR blocks from which NSX assigns IP addresses for Kubernetes Ingresses and Kubernetes Services of type LoadBalancer.
                      CIDR blocks from which NSX assigns IP addresses for Kubernetes Ingresses and Kubernetes Services of type LoadBalancer.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  mainDns:
                    description: |-
                      The list of addresses of the primary DNS servers.
                      List of DNS servers to use on the Kubernetes API server.
                    items:
                      type: string
                    type: array
                  managementNetwork:
                    description: |-
                      The configuration for the management network which the control plane VMs will be connected to.
                      The configuration for the management network which the control plane VMs will be connected to.
                    items:
                      properties:
                        addressCount:
                          description: |-
                            Number of addresses to allocate. Starts from starting_address
                            Number of addresses to allocate. Starts from 'starting_address'
                          type: number
                        gateway:
                          description: |-
                            Gateway IP address.
                            Gateway IP address.
                          type: string
                        network:
                          description: |-
                            ID of the network. (e.g. a distributed port group).
                            ID of the network. (e.g. a distributed port group).
                          type: string
                        startingAddress:
                          description: |-
                            Starting address of the management network range.
                            Starting address of the management network range.
                          type: string
                        subnetMask:
                          description: |-
                            Subnet mask.
                            Subnet mask.
                          type: string
                      type: object
                    type: array
                  namespace:
                    description: |-
                      The list of namespaces to create in the Supervisor cluster
                      List of namespaces associated with the cluster.
                    items:
                      properties:
                        contentLibraries:
                          description: |-
                            The list of content libraries to associate with the namespace
                            A list of content libraries.
                          items:
                            type: string
                          type: array
                        name:
                          description: |-
                            The name of the namespace
                            The name of the namespace.
                          type: string
                        vmClasses:
                          description: |-
                            The list of virtual machine classes to add to the namespace
                            A list of virtual machine classes.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  podCidr:
                    description: |-
                      CIDR blocks from which Kubernetes allocates pod IP addresses. Minimum subnet size is 23.
                      CIDR blocks from which Kubernetes allocates pod IP addresses. Minimum subnet size is 23.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  searchDomains:
                    description: |-
                      List of DNS search domains.
                      List of DNS search domains.
                    items:
                      type: string
                    type: array
                  serviceCidr:
                    description: |-
                      CIDR block from which Kubernetes allocates service cluster IP addresses.
                      CIDR block from which Kubernetes allocates service cluster IP addresses.
                    items:
                      properties:
                        address:
                          description: Network address.
                          type: string
                        prefix:
                          description: Subnet prefix.
                          type: number
                      type: object
                    type: array
                  sizingHint:
                    description: |-
                      The size of the Kubernetes API server.
                      Size of the Kubernetes API server.
                    type: string
                  storagePolicy:
                    description: |-
                      The name of the storage policy.
                      The name of a storage policy associated with the datastore where the container images will be stored.
                    type: string
                  workerDns:
                    description: |-
                      The list of addresses of the DNS servers to use for the worker nodes.
                      List of DNS servers to use on the worker nodes.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
